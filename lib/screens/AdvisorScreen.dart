import 'package:flutter/material.dart';

class AdvisorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Advisor')),
      body: Center(child: Text('Advisor Page')),
    );
  }
}
